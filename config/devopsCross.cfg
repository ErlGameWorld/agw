%% 服务器运维相关配置

% 服务器类型[game | center | cross]
{type, cross}.

% 游戏服id[平台id(2位)+区服id(5位)]
{server_id, 1}.

% 服务器地址
{host, "127.0.0.1"}.

% 游戏服端口
{port, 0}.

% web 端口
{web_port, 0}.

% ws 端口
{ws_port, 0}.

% 开服时间
{opened, {{2021, 4, 22}, {8, 8, 8}}}.