%% 服务器运维相关配置

% 服务器类型[game | center | cross]
{type, game}.

% 游戏服id[平台id(2位)+区服id(5位)]
{server_id, 1}.

% 服务器地址
{host, "127.0.0.1"}.

% 游戏服端口 (TCP)
{port, 9100}.

% web 端口 (HTTP)
{web_port, 9200}.

% ws 端口 (WebSocket)
{ws_port, 9300}.

% 开服时间
{opened, {{2021, 4, 22}, {8, 8, 8}}}.